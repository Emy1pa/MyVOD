import { View, Text } from "react-native";
import "@/global.css";

export default function MoviesScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black">
      <Text className="text-white text-xl">Movies Screen</Text>
    </View>
  );
}
